// import Vuex from 'vuex';
// import { mount, createLocalVue } from '@vue/test-utils';
import FilterChartComponent from '../components/vue-components/filter-employees.vue';
// import store from '.';

// const getters = store.getters;
// const localVue = createLocalVue();
// localVue.use(Vuex);

describe('filteredByDate', () => {
  it('returns data by Date', () => {
    console.log(FilterChartComponent);
    // const data = [dogs[0], dogs[2]];
    // const actual = getters.poodlesByAge(state, { poodles })(1);

    // expect(actual).toEqual([data[0]]);
  });
});
